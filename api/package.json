{
  "name": "api",
  "contributors": [
    "Italo Jose <italo.i@live.com>"
  ],
  "license": "UNLICENSED",
  "private": true,
  "engines": [
    "node >= 8.9.4",
    "npm >= 5.6.0"
  ],
  "scripts": {
    "lint": "standard --global after --global afterEach --global before --global beforeEach --global describe --global it",
    "start:debug": "DEBUG=api:* nodemon --watch app --watch config.js node bin/www",
    "start": "node bin/www",
    "test": "mocha test --recursive"
  },
  "dependencies": {
    "@mantris/appfy": "^0.2.10",
    "axios": "^0.18.0",
    "express-rescue": "^1.0.11",
    "mongoose": "^5.0.3",
    "sugar-env": "^1.1.0"
  },
  "devDependencies": {
    "nodemon": "^1.17.1",
    "standard": "^10.0.3"
  }
}
